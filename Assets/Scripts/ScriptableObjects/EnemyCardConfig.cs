using UnityEngine;

[CreateAssetMenu(fileName = "New Enemey Card Config", menuName = "Configs/Card")]
public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
